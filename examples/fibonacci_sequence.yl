fn fib(n: number): number {
    if n == 0 || n == 1 {
        return n;
    }

    return fib(n - 1) + fib(n - 2);
}

fn main(): void {
    var i = 0;
    while i < 20 {
        println(fib(i));
        i = i + 1;
    }
}

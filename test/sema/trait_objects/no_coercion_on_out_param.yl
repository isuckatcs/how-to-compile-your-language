// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {}

extension S : A {}

fn foo(x: &*any A) {}

fn main() {
  mut s = gc(S{});

  // CHECK: [[# @LINE + 1 ]]:7: error: cannot unify '*S' with '&*any A'
  foo(s);
}

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn main() {
  let l = ->(x){};
  // CHECK: FunctionDecl @({{.*}}) __builtin_lambda_call {((lambda@<source>:3:11), number) -> unit}

  ->() {
    let f: (number) -> unit = l;
  };
}

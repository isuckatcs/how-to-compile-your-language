// RUN: compiler %s -res-dump 2>&1 | filecheck %s
fn main() {
  let f: (&number) -> unit = ->(mut x) {};
  // CHECK: [[# @LINE - 1 ]]:37: error: a '&' parameter cannot be 'mut'

  let f2: (&mut number) -> unit = ->(mut x) {};
  // CHECK: [[# @LINE - 1 ]]:42: error: a '&' parameter cannot be 'mut'
}

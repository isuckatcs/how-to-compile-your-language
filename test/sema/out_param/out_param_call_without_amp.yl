// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn bar(x: borrowed mut number) {
  x = 1;
}

fn main() {
  mut x = 0;

  bar(x);
  // CHECK: CallExpr {unit}
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) bar {(borrowed mut number) -> unit}
  // CHECK-NEXT:   ImplicitBorrowExpr {borrowed mut number}
  // CHECK-NEXT:     DeclRefExpr @({{.*}}) x {number}
}

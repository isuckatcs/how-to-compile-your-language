// RUN: compiler %s -mono-dump 2>&1 | filecheck %s
fn foo<T>() {
  // CHECK: [[# @LINE + 1 ]]:3: error: overflow while instantiating 'foo' with '[T = **********bool]'
  foo@<*T>();
}

fn main() {
  if false {
    foo@<bool>();
  }
}

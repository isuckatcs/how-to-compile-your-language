// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {
  fn foo() {
    ->(){
      Self::foo;
      // CHECK: DeclRefExpr @({{.*}}) foo {() -> unit}
    };
  }
}

struct S {
  fn foo() {
    ->(){
      Self::foo;
      // CHECK: DeclRefExpr @({{.*}}) foo {() -> unit}
    };
  }
}

fn main() {}

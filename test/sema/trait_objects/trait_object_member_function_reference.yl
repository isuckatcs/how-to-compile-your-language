// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {
  fn foo(self: borrowed Self, x: number) {}
}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:18: error: expected to call member function on trait object
  @<impl A : A>::foo;
}

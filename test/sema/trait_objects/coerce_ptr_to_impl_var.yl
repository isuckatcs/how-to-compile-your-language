// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {}

extension S : A {}

fn main() {
  let x: *any A = gc(S{});
  // CHECK: VarDecl @({{.*}}) x {*any A}
  // CHECK-NEXT:   TraitObjectPromoExpr {*any A}
  // CHECK-NEXT:     GCExpr {*S}
}

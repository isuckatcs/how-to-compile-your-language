// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {
  impl A;
}

fn foo(): *impl A {
  return gc(S{});
  // CHECK: ReturnStmt
  // CHECK-NEXT:   TraitObjectPromoExpr {*impl A}
  // CHECK-NEXT:     GCExpr {*S}
}

fn main() {}

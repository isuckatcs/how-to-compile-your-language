// RUN: compiler %s -verify-only
// RUN: compiler %s -llvm-dump 2>&1 | filecheck %s
// RUN: compiler %s -o %t && %t | grep -Plzx '123\n123\n123\n123\n123\n123\n'

// CHECK: @vtable.T1AGS1SGnES1SGnEE = internal constant [1 x ptr] [ptr @_YT1AGS1SGnES1SGnEE3foo]
// CHECK: @vtable.T1AGS1SGuES1SGuEE = internal constant [1 x ptr] [ptr @_YT1AGS1SGuES1SGuEE3foo]
// CHECK: @vtable.T1AGS1SGFRuES1SGFRuEE = internal constant [1 x ptr] [ptr @_YT1AGS1SGFRuES1SGFRuEE3foo]

trait A<T> {
  fn foo(self: borrowed Self, t: T);
}

struct S<T> {
  x: T
}

extension<T> S<T> : A<S<T>> {
  fn foo(self: borrowed Self, t: S<T>) {
    println(123);
  }
}

fn foo<T : A<T>>(t: T) {
  let x: *any A<T> = gc(t);
  x.foo(t);
}

struct X {
  x: number
}

fn main() {
  let sn = S@<number>{ x: 123 };
  foo(sn);
  sn.foo(sn);
  // CHECK: call void @_YT1AGS1SGnES1SGnEE3foo(ptr %sn, ptr byval({ double }) %struct.arg.tmp1, ptr null)

  let su = S@<unit>{ x: unit };
  foo(su);
  su.foo(su);
  // CHECK: call void @_YT1AGS1SGuES1SGuEE3foo(ptr %su, ptr null)

  let sf = S@<() -> unit>{ x: main };
  foo(sf);
  sf.foo(sf);
  // CHECK: call void @_YT1AGS1SGFRuES1SGFRuEE3foo(ptr %sf, ptr byval({ { ptr, ptr } }) %struct.arg.tmp4, ptr null)
}

// CHECK: define void @_Y3fooGS1SGnEE(ptr byval({ double }) %t, ptr %closure) gc "shadow-stack" {
// CHECK:   %vFnPtr = getelementptr ptr, ptr %vtable, i32 0
// CHECK:   %vFn = load ptr, ptr %vFnPtr, align 8
// CHECK:   call void %vFn(ptr %obj, ptr byval({ double }) %struct.arg.tmp, ptr null)
// CHECK: }

// CHECK: define void @_Y3fooGS1SGuEE(ptr %closure) gc "shadow-stack" {
// CHECK:   %vFnPtr = getelementptr ptr, ptr %vtable, i32 0
// CHECK:   %vFn = load ptr, ptr %vFnPtr, align 8
// CHECK:   call void %vFn(ptr %obj, ptr null)
// CHECK: }

// CHECK: define void @_Y3fooGS1SGFRuEE(ptr byval({ { ptr, ptr } }) %t, ptr %closure) gc "shadow-stack" {
// CHECK:   %vFnPtr = getelementptr ptr, ptr %vtable, i32 0
// CHECK:   %vFn = load ptr, ptr %vFnPtr, align 8
// CHECK:   call void %vFn(ptr %obj, ptr byval({ { ptr, ptr } }) %struct.arg.tmp, ptr null)
// CHECK: }

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {}

extension S : A {}

struct S2 {
  x: *any A
}

fn main() {
  S2 { x: gc(S{}) };
  // CHECK: FieldInitStmt @({{.*}}) x
  // CHECK-NEXT:   TraitObjectPromoExpr {*any A}
  // CHECK-NEXT:     GCExpr {*S}
}

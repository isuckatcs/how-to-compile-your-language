// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {}

extension S : A {}

fn foo(): *any A {
  return gc S{};
  // CHECK: ReturnStmt
  // CHECK-NEXT:   TraitObjectPromoExpr {*any A}
  // CHECK-NEXT:     GCExpr {*S}
}

fn main() {}

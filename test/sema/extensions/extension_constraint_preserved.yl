// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A {}

trait B {}

struct S<T> {}

extension<T : A> S<T> {
  fn foo(t: T) {}
}

fn main() {
  let f = S::foo;
  
  // CHECK: [[# @LINE + 1 ]]:5: error: cannot satisfy requirement 'number : A'
  f(123);
}

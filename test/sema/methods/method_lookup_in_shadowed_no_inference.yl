// RUN: compiler %s 2>&1 -res-dump | filecheck %s
struct shadowed<T, U> {
  fn method() {}
}

fn shadowed<T>() {}

fn main(){
  // FIXME: only report it for the parent
  shadowed::method();
  // CHECK: [[# @LINE - 1 ]]:3: error: explicit type annotations are needed to infer the type of 'shadowed'
  // CHECK: [[# @LINE - 2 ]]:13: error: explicit type annotations are needed to infer the type of 'method'
}

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {}

extension S : A {}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:20: error: an expression of type '**S' cannot be used to initialize a variable of type '**any A'
  let s: **any A = gc(gc(S{}));
}

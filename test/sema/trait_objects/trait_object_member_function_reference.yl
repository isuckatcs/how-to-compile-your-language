// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {
  fn foo(self: &Self, x: number) {}
}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:17: error: expected to call member function on trait object
  @<any A : A>::foo;
}

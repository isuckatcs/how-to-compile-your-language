// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {
  impl A;
}

fn main() {
  let x: *impl A = gc(S{});
  // CHECK: VarDecl @({{.*}}) x {*impl A}
  // CHECK-NEXT:   TraitObjectPromoExpr {*impl A}
  // CHECK-NEXT:     GCExpr {*S}
}

// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A {}

struct S<T : A> {}

extension<T : A> S<T> {
  fn foo(x: T) {}
}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:10: error: cannot satisfy requirement 'number : A'
  S::foo(0);
}

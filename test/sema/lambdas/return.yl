// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn foo(): (number) -> number {
  return ->(x){ return x; };
  // CHECK: ReturnStmt
  // CHECK-NEXT:   LambdaExpr {(number) -> number}
  // CHECK-NEXT:     StructDecl @({{.*}}) (lambda@<source>:3:10) {(lambda@<source>:3:10)}
}

fn main() {}

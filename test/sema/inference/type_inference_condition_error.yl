// RUN: compiler %s -res-dump 2>&1 | filecheck %s
struct S {
  x: number
}

fn main(): unit { 
  let s;

  if s {}

  // CHECK: [[# @LINE + 1 ]]:9: error: cannot unify 'S' with 'bool'
  s = S { x: 1 };
}

// CHECK-NOT: {{.*error.*}}

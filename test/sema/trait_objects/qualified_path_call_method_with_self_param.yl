// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {
  fn val(self: borrowed Self) {}
}

struct S {
  impl A;
}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:26: error: only lvalues can be borrowed
  @<impl A impl A>::val(S{});
}

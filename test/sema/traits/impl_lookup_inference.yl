// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A<T> {
  fn foo() {}
}

struct S<T> {
  impl A<T>;
}

fn main() {
  @<S<number> : A<number>>::foo();
  // CHECK: CallExpr {unit}
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) @<S<number> : A<number>>::foo {() -> unit}
}

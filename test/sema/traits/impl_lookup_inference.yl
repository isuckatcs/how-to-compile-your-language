// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A<T> {
  fn foo() {}
}

struct S<T> {}

extension<T> S<T> : A<T> {}

fn main() {
  @<S<number> : A<number>>::foo();
  // CHECK: CallExpr {unit}
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) foo {() -> unit}
}

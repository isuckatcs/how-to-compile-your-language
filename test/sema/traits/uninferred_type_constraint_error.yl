// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A {}

trait B {}

fn foo<T : A>(t: T): T {
  return t;
}

fn bar<T : B>(t: T): T {
  return t;
}

fn main() {
  mut x = foo;
  x = bar;
  // CHECK: [[# @LINE - 1 ]]:3: error: unknown function type '(_) -> _' on the lhs of assignment
}

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn main() {
  mut f: (number) -> number = ->(x){ return x; };
  // CHECK: VarDecl @({{.*}}) f {(number) -> number}
  // CHECK-NEXT:   LambdaExpr {(number) -> number}
  // CHECK-NEXT:     StructDecl @({{.*}}) (lambda@<source>:3:31) {(lambda@<source>:3:31)}

  f = ->(x){ return x; };
  // CHECK: Assignment
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) f {(number) -> number}
  // CHECK-NEXT:   LambdaExpr {(number) -> number}
  // CHECK-NEXT:     StructDecl @({{.*}}) (lambda@<source>:8:7) {(lambda@<source>:8:7)}
}

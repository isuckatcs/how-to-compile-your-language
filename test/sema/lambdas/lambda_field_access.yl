// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn main() {
  let n = 0;
  
  // CHECK: [[# @LINE + 1 ]]:21: error: cannot look up field 'n' in primitive type '() -> number'
  ->(){ return n; }.n;
}

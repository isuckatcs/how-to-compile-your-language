// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

fn foo(a: &any A) {}

fn main() {
  let n = 123;

  // CHECK: [[# @LINE + 1 ]]:7: error: cannot satisfy requirement 'number : A'
  foo(n);
}

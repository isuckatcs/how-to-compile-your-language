// RUN: compiler %s -verify-only
// RUN: compiler %s -llvm-dump 2>&1 | filecheck %s
// RUN: compiler %s -o %t && %t | grep -Plzx '123\n'

fn main() {
  let u1 = unit;
  let u2 = unit;

  if u1 == u2 {
    println(123);
  }
  // CHECK: br i1 true, label %if.true, label %if.exit
}

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn foo(): (number) -> number {
  return ->(x){ return x; };
  // CHECK: ReturnStmt
  // CHECK-NEXT:   LambdaExpr {(number) -> number}
  // CHECK-NEXT:     StructDecl @({{.*}}) (closure@<source>:3:10) {(closure@<source>:3:10)}
}

fn main() {}

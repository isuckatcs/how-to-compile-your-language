// RUN: compiler %s -res-dump 2>&1 | filecheck %s
struct S {
  x: number
}

fn main(): unit { 
  mut s;
  s = 0;

  // CHECK: [[# @LINE + 1 ]]:9: error: cannot unify 'S' with 'number'
  s = S { x: 1 };
}

// CHECK-NOT: {{.*error.*}}

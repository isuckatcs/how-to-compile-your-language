// RUN: compiler %s -res-dump 2>&1 | filecheck %s
struct S<T> {
  x: T
}

fn main(): unit {
  let s = S{ x: S{ x: 1 } };
  
  // CHECK: [[# @LINE + 1 ]]:15: error: cannot assign to immutable value
  (((s.x).x)) = 2;
}

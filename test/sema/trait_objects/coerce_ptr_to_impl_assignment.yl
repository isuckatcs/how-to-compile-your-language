// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

struct S {
  impl A;
}

fn foo(mut x: *any A) {
  x = gc(S{});
  // CHECK: Assignment
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) x {*any A}
  // CHECK-NEXT:   TraitObjectPromoExpr {*any A}
  // CHECK-NEXT:     GCExpr {*S}
}

fn main() {}

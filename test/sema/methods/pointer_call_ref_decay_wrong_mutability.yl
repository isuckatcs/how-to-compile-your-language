// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {
  fn foo(self: &mut Self) {}
}

struct S {}

extension S : A {}

fn main() {
  let s = gc S{};

  // CHECK: [[# @LINE + 1 ]]:3: error: cannot unify '*S' with '&mut S'
  s.foo();
}

// RUN: compiler %s 2>&1 -res-dump | filecheck %s
trait A {}

extension number : A {}

fn foo(n: &mut number) {
  bar(n);
  // CHECK: CallExpr {unit}
  // CHECK-NEXT:   DeclRefExpr @({{.*}}) bar {(&any A) -> unit}
  // CHECK-NEXT:   TraitObjectPromoExpr {&any A}
  // CHECK-NEXT:     DeclRefExpr @({{.*}}) n {&mut number}
}

fn bar(a: &any A) {}

fn main() {}

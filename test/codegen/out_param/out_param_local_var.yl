// RUN: compiler %s -verify-only
// RUN: compiler %s -llvm-dump 2>&1 | filecheck %s
// RUN: compiler %s -o %t && %t | grep -Plzx '0\n10\n'
fn bar(x: &mut number) {
  x = 10;
}
// CHECK: define void @bar(ptr %x, ptr %closure) {
// CHECK-NEXT: entry:
// CHECK-NEXT:   store double 1.000000e+01, ptr %x, align 8
// CHECK-NEXT:   ret void
// CHECK-NEXT: }

fn main() {
  mut x = 0;
  // CHECK: %x = alloca double, align 8
  // CHECK-NEXT: store double 0.000000e+00, ptr %x, align 8

  println(x);

  bar(&x);
  // CHECK: call void @bar(ptr %x, ptr null)

  println(x);
}

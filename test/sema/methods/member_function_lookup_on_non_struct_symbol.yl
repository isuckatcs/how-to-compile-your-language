// RUN: compiler %s 2>&1 -res-dump | filecheck %s
struct S {}

fn foo(p: S) {
  let v = S{};

  // CHECK: [[# @LINE + 1 ]]:3: error: cannot look up member in value
  v::foo();

  // CHECK: [[# @LINE + 1 ]]:3: error: cannot look up member in value
  p::foo();
}

fn main() {}

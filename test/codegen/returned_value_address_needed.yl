// RUN: compiler %s -verify-only
// RUN: compiler %s -llvm-dump 2>&1 | filecheck %s
// RUN: compiler %s -o %t && %t | ( ! grep ^ )
struct S {}

fn consume(x: &unit, y: &S) {}

fn foo() {}

fn bar(): S {
  return S{};
}

fn main() {
  let u = foo();
  let s = bar();

  consume(u, s);
}
// CHECK: define void @_Y4main(ptr %closure) {
// CHECK-NEXT: entry:
// CHECK-NEXT:   %u = alloca [0 x i8], align 1
// CHECK-NEXT:   %s = alloca {}, align 8
// CHECK-NEXT:   call void @_Y3foo(ptr null)
// CHECK-NEXT:   call void @_Y3bar(ptr null)
// CHECK-NEXT:   call void @_Y7consume(ptr %u, ptr %s, ptr null)
// CHECK-NEXT:   ret void
// CHECK-NEXT: }

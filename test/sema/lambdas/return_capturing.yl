// RUN: compiler %s 2>&1 -res-dump | filecheck %s
fn foo(): (number) -> number {
  let n = 0;

  return ->(x){ return n; };
  // CHECK: ReturnStmt
  // CHECK-NEXT:   LambdaExpr {(number) -> number}
  // CHECK-NEXT:     DeclRefExpr @({{.*}}) n {number}
  // CHECK-NEXT:     StructDecl @({{.*}}) (closure@<source>:5:10) {(closure@<source>:5:10)}
}

fn main() {}

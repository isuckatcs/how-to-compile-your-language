// RUN: compiler %s -res-dump 2>&1 | filecheck %s
trait A<T> {}

struct S<T> {}

extension<T, U : A<S<T>>> U : A<T> {}

fn foo<T, U : A<T>>(u : U) {}

fn main() {
  // CHECK: [[# @LINE + 1 ]]:7: error: cannot satisfy requirement 'number : A<_>'
  foo(1);
}

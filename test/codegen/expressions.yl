// RUN: compiler %s -o expressions && ./expressions | grep -Plzx '\-9\n-16\n-15\n'
fn foo(x: number): number {
    return (3.0 * x / (1.5 + 1.5)) - 10.0;
}

fn main(): void {
    println(foo(1));
    println(foo(-6));
    println(foo(-5));
}
